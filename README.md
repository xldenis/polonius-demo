# Making lifetimes visible

This repo is an experiment in using Polonius to make lifetimes apparent in MIR.

--

It doesn't *fully* work yet though.

-- 

```
cargo run -- demo.rs --crate-type=lib
```
